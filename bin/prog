#!/usr/bin/env ruby
name = "app/views/pages/prefectures.html.haml"
file = File.open(name)
data = file.read

data.gsub!(/(-?\d+\.\d{3,6})/) do |m|
  $1.to_f.round(1).to_s
end

File.open(name, "w") do |f|
  f.write data
end
