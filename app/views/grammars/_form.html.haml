:ruby
  inputs = [
    [:title,   t("grammar.title"),   :text],
    [:jregexp, t("grammar.jregexp"), :text],
    [:eregexp, t("grammar.eregexp"), :text],
    [:note,    t("grammar.note"),    :area, rows: 30],
  ]

= render "utils/crud/form", object: grammar, index: grammars_path, inputs: inputs, title: title
