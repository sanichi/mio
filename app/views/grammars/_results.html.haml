- grammars.matches.each do |grammar|
  %tr
    %td= link_to grammar.title, grammar
    %td.text-center= "#{grammar.examples.size}/#{grammar.last_example_checked}"
    %td.text-center= render "level", level: grammar.level
= render "utils/pagination_links", pager: grammars, columns: 3
