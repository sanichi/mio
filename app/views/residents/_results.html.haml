- residents.matches.each do |resident|
  %tr
    %td= link_to resident.name, resident
    %td.text-center= resident.block
    %td.text-center= resident.flat
    %td= resident.email
= render "utils/pagination_links", pager: residents, columns: 4
