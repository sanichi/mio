:ruby
  inputs = [
    [:name,     t("star.name"),     :text,  maxlength: Star::MAX_NAME],
    [:distance, t("star.distance"), :text],
    [:note,     t("star.note"),     :area,  rows: 20],
  ]
= render "utils/crud/form", object: star, index: stars_path, inputs: inputs, title: title
