.row.top_margin
  .col-xs-12
    %table{class: "table table-bordered table-striped"}
      %thead
        %tr
          %th.col-xs-8= t("vocab.meaning")
          %th.col-xs-3= t("vocab.kanji")
          %th.col-xs-1
      %tbody
        - test.vocab_questions.each do |q|
          %tr
            %td= q.vocab.meaning
            %td= q.kanji
            %td.text-center= t("symbol.#{q.kanji_correct ? 'tick' : 'cross'}")
