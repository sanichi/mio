module Misc where

import Dict exposing (Dict)

highPriority : Int
highPriority = <%= @priority_high %>

lowPriority : Int
lowPriority = <%= @priority_low %>

priorities : Dict Int String
priorities =
  Dict.fromList [ <%= @priority_desc.each_pair.map{ |k, v| '(%d, "%s")' % [k, v] }.join(", ") %> ]
