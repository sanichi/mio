= render "utils/title", key: "wk.radical.radicals"

= form_tag wk_radicals_path, method: :get, class: "form-inline justify-content-center" do
  = label_tag :rquery, t("wk.radical.radical"), class: "sr-only"
  = text_field_tag :rquery, params[:rquery], class: "form-control mr-1 mb-1", placeholder: t("wk.radical.radical"), size: 16, autofocus: true
  = label_tag :id, t("wk.id"), class: "sr-only"
  = text_field_tag :id, params[:id], class: "form-control mr-1 mb-1", placeholder: t("wk.short.id"), size: 5
  = label_tag :level, t("wk.level"), class: "sr-only"
  = select_tag :level, wk_level_menu(params[:level]), class: "form-control mr-1 mb-1 auto-submit"
  = label_tag :order, t("order"), class: "sr-only"
  = select_tag :order, wk_radical_order_menu(params[:order]), class: "form-control mr-1 mb-1 auto-submit"
  = button_tag t("search"), type: "submit", class: "btn btn-success mr-1 mb-1"

.row
  .offset-lg-1.col-lg-10.offset-xl-2.col-xl-8
    %table{class: "table table-bordered table-striped table-sm mt-3"}
      %thead
        %tr.d-flex
          %th.col-2.text-center= t("wk.radical.name")
          %th.col-1.text-center= t("wk.short.character")
          %th.col-8.text-center= t("wk.hint.mnemonic")
          %th.col-1.text-center= t("wk.level")
      %tbody
        = render "results", radicals: @radicals

.text-center
  = link_to t("wk.kanji.kanjis"), last_search(wk_kanjis_path), class: "btn btn-success"
  = link_to t("wk.vocab.vocabs"), last_search(wk_vocabs_path), class: "btn btn-success"
