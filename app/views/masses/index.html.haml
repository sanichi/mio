= render "utils/title", key: "mass.data"

.row
  .col-md-offset-3.col-md-6
    %table{class: "table table-bordered table-striped"}
      %thead
        %tr
          %th.text-center= t("mass.date")
          %th.text-center= t("mass.start")
          %th.text-center= t("mass.finish")
          %th
      %tbody
        - @masses.matches.each do |mass|
          %tr
            %td.text-center= mass.date
            %td.text-center= "%.1f" % mass.start if mass.start.present?
            %td.text-center= "%.1f" % mass.finish if mass.finish.present?
            %td.text-center
              = link_to t("edit"), edit_mass_path(mass), class: "btn btn-info btn-xs"
        = render "utils/pagination_links", pager: @masses, columns: 4

.text-center
  = link_to t("graph"), graph_masses_path, class: "btn btn-default"
  = link_to t("mass.new"), new_mass_path, class: "btn btn-default"
