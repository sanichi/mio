- tx = bw/2
- ty = bh/2

%g{id: "bay-#{id}", class: "bay", transform: "translate(#{bw * i},0)", "data-bay" => id}
  %rect{width: bw, height: bh}
  %text{x: tx, y: ty + 5, transform: "rotate(#{-r},#{tx},#{ty})"}= id
