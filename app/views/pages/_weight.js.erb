$(function() {
  Elm.Weight.init({
    node: $('#elm-app').get(0),
    flags: {
      debug: true,
      months: 5,
      unit: 'st',
      kilos: [<%= @kilos.map(&:to_s).join(',') %>],
      dates: ['<%= @dates.map(&:to_s).join("','").html_safe %>']
    }
  });
});
