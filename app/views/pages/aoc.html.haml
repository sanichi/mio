= render "utils/title", key: "pages.aoc.title", header: true

- content_for :javascript do
  = render partial: "aoc_elm_min", formats: :js
  = render partial: "aoc", formats: :js, locals: { year: params[:year], day: params[:day] }

%hr

.div{id: "elm-app"}
