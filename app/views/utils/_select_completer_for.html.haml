- defined?(cwidth) || cwidth = 1

.form-group{class: object.errors[param].any? ? "has-error" : ""}
  = form.label param, text, class: "control-label col-#{col}-#{pad}"
  %div{class: "col-#{col}-#{width}"}= form.select param, menu, {}, class: "form-control input-sm"
  %div{class: "col-#{col}-#{cwidth}"}
    .form-inline
      .input-group.input-group-sm
        = text_field_tag :term, "", id: "#{param}_completer", class: "form-control", size: "100%"
  %div{class: "col-#{col}-#{12-pad-width-cwidth} help-block"}= object.errors[param].first
