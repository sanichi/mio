.form-group.row{class: object.errors[param].any? ? "has-error" : ""}
  = form.label param, text, class: "col-form-label col-#{col}-#{pad}"
  %div{class: "col-#{col}-#{width}"}= form.file_field param, class: "form-control"
  %div{class: "col-#{col}-#{12-pad-width} help-block"}= object.errors[param].first
