- defined?(multiple) || multiple = false
- defined?(auto)     || auto = true
- auto_submit = auto && !multiple ? "auto-submit" : ""

= select_tag param, menu, class: "form-select #{auto_submit}", multiple: multiple
