- interfaces.matches.each do |interface|
  %tr
    %td= link_to interface.name, interface
    %td= interface.mac_address
    %td= interface.ip_address
    %td= interface.manufacturer
= render "utils/pagination_links", pager: interfaces, columns: 4
